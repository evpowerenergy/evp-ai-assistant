diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index 5ba09d6..0a3b1d2 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -2187,6 +2187,16 @@ class APIServerAdapter(BasePlatformAdapter):
             session_id = _derive_chat_session_id(system_prompt, first_user)
             # history already set from request body above

+        # EVP integration: bind the signed capability to the trusted session
+        # out-of-band. It is never added to messages, schemas, or model input.
+        execution_token = request.headers.get("X-EVP-Execution-Token", "").strip()
+        if execution_token:
+            from evp_execution_context import bind_execution_token
+            if not bind_execution_token(gateway_session_key or session_id, execution_token):
+                return web.json_response(
+                    _openai_error("Invalid X-EVP-Execution-Token header"), status=400
+                )
+
         completion_id = f"chatcmpl-{uuid.uuid4().hex[:29]}"
         model_name = body.get("model", self._model_name)
         created = int(time.time())
diff --git a/tools/mcp_tool.py b/tools/mcp_tool.py
index cd999f2..2a5ca91 100644
--- a/tools/mcp_tool.py
+++ b/tools/mcp_tool.py
@@ -3940,7 +3940,21 @@ def _make_tool_handler(server_name: str, tool_name: str, tool_timeout: float):
                 # it and detect the gateway platform / session for routing.
                 server._pending_call_context = contextvars.copy_context()
                 try:
-                    result = await server.session.call_tool(tool_name, arguments=args)
+                    call_args = dict(args or {})
+                    if server_name == os.environ.get("EVP_MCP_SERVER_NAME", "evp"):
+                        from evp_execution_context import get_execution_token
+                        from gateway.session_context import get_session_env
+                        execution_token = get_execution_token(
+                            get_session_env("HERMES_SESSION_KEY")
+                            or get_session_env("HERMES_SESSION_ID")
+                            or ""
+                        )
+                        if not execution_token:
+                            return json.dumps({
+                                "error": "Missing trusted EVP execution context"
+                            }, ensure_ascii=False)
+                        call_args["evp_execution_token"] = execution_token
+                    result = await server.session.call_tool(tool_name, arguments=call_args)
                 finally:
                     server._pending_call_context = None
             # MCP CallToolResult has .content (list of content blocks) and .isError
@@ -4498,7 +4512,14 @@ def _convert_mcp_schema(server_name: str, mcp_tool) -> dict:
     prefixed_name = mcp_prefixed_tool_name(server_name, mcp_tool.name)
+    parameters = _normalize_mcp_input_schema(getattr(mcp_tool, "inputSchema", None))
+    if server_name == os.environ.get("EVP_MCP_SERVER_NAME", "evp"):
+        parameters.get("properties", {}).pop("evp_execution_token", None)
+        parameters["required"] = [
+            item for item in parameters.get("required", [])
+            if item != "evp_execution_token"
+        ]
     return {
         "name": prefixed_name,
         "description": mcp_tool.description or f"MCP tool {mcp_tool.name} from {server_name}",
-        "parameters": _normalize_mcp_input_schema(getattr(mcp_tool, "inputSchema", None)),
+        "parameters": parameters,
     }
